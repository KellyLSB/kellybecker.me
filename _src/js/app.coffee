do $(document).foundation

# $ ->
#   params = $.deparam.querystring true
#   console.log params
#
#   if params['details'] is 'extended'
#     do $('div.description.paragraphs').show
#
#    $('li a').each ->
#      if $(@).attr 'href' is '?' + qs
#        $(this).addClass 'current'
